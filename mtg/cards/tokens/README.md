# Tokens

Package `tokens` collects generated definitions for playable paper tokens. Token definitions live in letter subpackages and use their complete Oracle ID in filenames and Go identifiers so same-name tokens remain distinct.

Tokens are not included in `cards.Registry`. In the repository tree, use `tokens.Cards` when all token definitions are needed.
